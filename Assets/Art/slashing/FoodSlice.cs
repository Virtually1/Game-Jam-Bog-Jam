
using UnityEngine;

public class FoodSlice : MonoBehaviour
{
    // Start is called before the first frame update
    public Texture[] textures;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
